export enum RunnerType {
  NetTool = "Cake .NET Tool (Global)",
  NetFramwork = "Cake runner for .NET Framework",
  NetCore = "Cake runner for .NET Core"
}

export enum DebugType {
  NetTool = "Cake .NET Tool (Global)",
  NetCore = "Cake runner for .NET Core"
}